;The comment below contains SFX script commands
Overwrite=1
Delete=distribution\*.*
Title=CyberCTR Update Package 1.2.1
Text
{
<html>
<body>
<h2>How to use:</h2>
<div style="font-size:14px;">
<h4>For installed and zipped:</h4>
<ul>
<li>Download and run <em>CyberCTRUpdatePackage.exe</em></li>
<li>Then on the open window click the browse button and select cyberfox install folder or the folder that contains <em>cyberfox.exe</em></li>
<li>Now Once you have selected the install folder or the folder that contains <em>cyberfox.exe</em></li>
<li>Click the extract button.</li>
<li>It will then remove the old versions of CyberCTR and add the newer version.</li>
</ul>
<h4>For cyberfox portable and zipped portable:</h4>
<ul>
<li>Download and run <em>CyberCTRUpdatePackage.exe</em></li>
<li>Then on the open window click the browse button and select CyberfoxPortable\App\Cyberfox</li>
<li>Now Once you have selected CyberfoxPortable\App\Cyberfox</li>
<li>Click the extract button.</li>
<li>It will then remove the old versions of CyberCTR and add the newer version.</li>
</ul>
</div>
</body>
<html>
}